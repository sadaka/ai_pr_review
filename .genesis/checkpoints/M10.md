# M10 — Reliability under fault injection

## G0 · 2026-09-08
- **slice rationale:** the `M10+` PLAN/DONE row bundles phases 12, 17, 18, 20 (reliability / DX / CI-CD / continuous-learning). Sliced to **phase 12's green gate** — spec §4.1: *"Retries, circuit breakers, idempotency verified under fault injection."* Spec §4.2 module map: `reliability/` = `retry, circuit_breaker, idempotency, timeout` ("The L8 reliability mechanics"). DX / prompt playground / trace viewer → later (phase 17); CI/CD eval-gate wiring → later (phase 18); drift detection → later (phase 20).
- **wiki pages read:** [[wiki/index.md]] — no reliability concept page. Seeded pointer: `$AGENTIC_SWE_WIKI_ROOT/llmops-ai-agents/concepts/Production-Hardening.md` ("reliability layer — retries, circuit breakers, idempotency — Phase 12") and `$AGENTIC_SWE_WIKI_ROOT/distributed-systems/concepts/Fault-Tolerance.md` ("orchestration-deadlock and timeout defenses at the aggregator join, Phase 4/12"). [[concepts/Idempotent-Retry-Ordering]] directly relevant — the claim-before-act + rollback pattern already used at the webhook/queue boundary (M2).
- **spec re-read:** `ai-pr-review-agent.html` §4.1 phase 12 (line 1616 area) + §4.2 module map (`reliability/`, line ~1646). Architecture study — no detailed mechanics to mine; the four module names + the one-line gate + the `outbound_call_safety` context-graph invariant are the contract. §4.3 (Tiger integration) confirms greenfield, no zero-downtime constraint.
- **implementation-notes searched for:** "reliability", "circuit", "retry", "fault", "idempoten" — none as a dedicated module; scattered mentions in M2/M5 notes.
- **codebase grep — what already exists:**
  - `backend/reliability/` — **does not exist.**
  - **Two deliberately self-contained implementations:**
    - `job_queue/queue.py` — `CircuitBreaker` (dataclass; N-consecutive-failure open, cooldown → half-open probe), `_with_retry` (exp backoff, 3 attempts, re-raises last), `JobQueue._guarded()` wrapper. Redis `socket_connect_timeout=5` + `socket_timeout=5`. Idempotency: `mark_seen` claims the delivery id (`SET NX EX`), `unclaim` releases it on a failed enqueue. **Tested** — `test_job_queue.py` covers retry-then-succeed (asserts 3 calls), breaker-opens-after-threshold, half-open-after-cooldown, replay dedup.
    - `integrations/github_client.py` — `_CircuitBreaker` (near-identical, threshold 4), retry, `_is_retryable_status` (429 / ≥500), `httpx` `timeout=10`. Docstring: *"Same self-contained pattern as `job_queue/queue.py` — deliberately not shared, to avoid a sideways import."*
  - **LLM calls:** `agents/llm_client.py` — OpenAI SDK built-in `max_retries=3` (exp backoff) + `timeout=30`. **No circuit breaker.**
  - **THE GAP — Tiger Cloud / asyncpg calls have no retry and no breaker:**
    - `integrations/truth_store.py`, `economics/cost_repository.py`, `memory/context_retriever.py`, `api/app.py` — every pool has `command_timeout=10` (and some a `_init` codec setup) but **no retry-with-backoff, no circuit breaker**. `cost_repository.py:11` says so outright: *"pool carries `command_timeout`; retry/breaker deferred"*. M4/M5/M6/M7 checkpoints all defer "DB-call retry/breaker to L8/L12."
  - **No fault-injection tests.** `test_orchestrator.py` has a `"simulated crash"` for checkpoint-resume (M3) but nothing induces connection drops / command timeouts / transient 5xx / partial write failures against the retry/breaker paths.
  - **Idempotency — partial (M5 carried-forward debt):** `truth_store.upsert_review` has `ON CONFLICT (github_delivery_id)` (good), but `insert_findings` has **no unique constraint** on `finding_records` (a retried `aggregate()` after a crash between `insert_findings` and `mark_posted` double-inserts findings), no `SELECT … FOR UPDATE` in `upsert_review`, and `mark_posted` has a crash-window gap. These need a migration to fix.
- **context-graph / invariants that bind us:**
  - `outbound_call_safety` (the invariant this milestone *is*): *"Every outbound call (LLM provider, GitHub API, Tiger Cloud, Redis) has an explicit timeout, retry-with-backoff, and circuit breaker; every retried operation is idempotent"* — currently PARTIAL (Redis ✓, GitHub ✓, LLM timeout+retry / no breaker, **DB timeout only**).
  - `dependency_direction`: the existing breakers are self-contained precisely to avoid a sideways import. A shared `reliability/` module means either (a) `reliability/**` becomes a cross-cutting exception (like `observability/**`, `security/**`) — it would be imported by `integrations/`, `economics/`, `memory/`, `job_queue/`, `agents/` — or (b) `reliability/` sits at the `core/` layer (imports nothing under `backend/`) and everyone imports *inward* to it, which is actually clean since retry/breaker/timeout primitives have zero project dependencies. **(b) is the better story — `reliability/` is a leaf like `core/`, no exception needed.**
  - `single_data_spine` — not implicated (breaker state is in-process, ephemeral, never durable).
- **credentials:** none new. Fault-injection tests use fakes / monkeypatched failures — no live creds needed for the demo. An optional live e2e (induce a real command timeout against Tiger) would reuse `TIGER_DATABASE_URL`.
- **scope tension / design questions (surface to user):**
  1. **M10 = reliability only?** Recommend yes — `reliability/` module + fault-injection suite + close the DB-layer gap + the LLM breaker. DX/CI-CD/drift → later milestones.
  2. **Shared module vs. leave the two self-contained breakers?** Recommend: **build `reliability/` as a leaf module** (`retry.py`, `circuit_breaker.py`, `timeout.py`, `idempotency.py` — pure, zero project imports, sits at the `core/` layer so everyone imports *inward*, no `dependency_direction` exception). **Migrate the uncovered surface to it** (the asyncpg repos get a `reliability`-wrapped executor; add an LLM-client breaker). **Leave `job_queue` + `github_client` as-is** for now (they work, they're tested) but add a conformance test asserting all three breakers share the same behavioral contract; a follow-up can DRY them. Rationale: matches the M5/M6/M8 pattern (local Protocol + swappable + minimal blast radius), delivers the invariant, respects the existing deliberate choice.
  3. **Does M10 close the M5 idempotency debt (finding_records unique constraint, FOR UPDATE, mark_posted gap)?** Recommend **yes for the `finding_records` unique constraint** (it's squarely "idempotency verified under fault injection" and it's a small additive migration — `UNIQUE (review_id, agent_type, file, line_start, category)` or a content hash) + a regression test that a re-run `aggregate()` doesn't double-insert. `SELECT … FOR UPDATE` + the `mark_posted` crash window are deeper and depend on `aggregate()` getting a retrying caller (the still-missing ARQ worker) — recommend **documenting them as still-open**, not fixing blind here.
  4. **Fault-injection test posture.** A new `backend/tests/test_reliability_fault_injection.py` — pure/fake-driven, induces: retry-then-succeed (call count proves N attempts), retry-exhausted-then-raise, breaker-opens-and-fast-fails (no call past the threshold), breaker-half-opens-after-cooldown, command-timeout surfaces as the typed error, connection-drop mid-op is retried, **retried write has no duplicate side effect** (idempotency — the key assertion), LLM-client breaker trips after repeated 5xx. Plus the conformance test across the 3 breakers. Optional live e2e behind the usual skipif.
  5. **`timeout.py` — what's in it?** The asyncpg pools already pass `command_timeout`; `httpx`/OpenAI already take `timeout=`. Recommend `timeout.py` is a thin `asyncio.wait_for`-based `with_timeout(coro, seconds)` helper + the canonical timeout constants in one place (they're currently scattered as `_REQUEST_TIMEOUT_SECONDS` / `DB_COMMAND_TIMEOUT_SECONDS` / etc. per module) — a small consolidation, not a rewrite.
- **verdict:** **UNBUILT** → await design answers, then L1 BUILD.

## Micro-plan · 2026-09-08

**User design decisions (2026-09-08) — all "recommended":** (1) M10 = reliability only; DX/CI-CD/drift → later. (2) new `reliability/` **leaf** module (pure, zero project imports, `core/`-tier so all imports are inward — no `dependency_direction` exception); migrate the asyncpg/DB repos + add an LLM breaker; leave `job_queue`/`github_client`'s own tested breakers in place + a conformance test across all three. (3) add the `finding_records` unique constraint + a no-double-insert regression test; leave `SELECT … FOR UPDATE` + the `mark_posted` crash window documented as still-open. (4) fake-driven deterministic fault-injection suite; optional live e2e behind skipif.

**Freeze boundary (widened, user-approved via the design answers):** `backend/reliability/**` (new), `backend/tests/test_reliability_fault_injection.py` (new), `scripts/migrations/2026-09-reliability.sql` (new), and *wiring-only* edits to `integrations/truth_store.py`, `economics/cost_repository.py`, `memory/context_retriever.py`, `agents/llm_client.py`, `agents/base_agent.py`, `evaluation/judge.py`. `context-graph.json` invariant note updated (no new exception — `reliability/` is a documented leaf).

**Files:**
- `backend/reliability/__init__.py` — new package. Re-exports the public surface.
- `backend/reliability/circuit_breaker.py`:
  - `CircuitOpenError(RuntimeError)`.
  - `CircuitBreaker` — `__init__(*, name: str, failure_threshold: int = 5, reset_after_seconds: float = 30.0)`; `before_call()` (raise `CircuitOpenError` while open and inside cooldown; allow one probe past cooldown = half-open), `on_success()` (reset), `on_failure()` (count; open at threshold). `time.monotonic()`. `state` property (`"closed"|"open"|"half_open"`) for the conformance test + observability. This is the canonical shape `job_queue`/`github_client` already independently implement.
- `backend/reliability/retry.py`:
  - `async def retry_async(op, *, attempts: int = 3, base_delay: float = 0.1, max_delay: float = 5.0, retry_on: tuple[type[BaseException], ...] = (Exception,))` — exp backoff `min(base_delay * 2**i, max_delay)`, re-raises the last exception; a non-`retry_on` exception propagates immediately (no retry).
- `backend/reliability/timeout.py`:
  - `OperationTimeout(RuntimeError)`.
  - `async def with_timeout(coro, seconds: float, *, name: str = "")` — `asyncio.wait_for`, translate `asyncio.TimeoutError` → `OperationTimeout(f"{name} exceeded {seconds}s")`.
  - Canonical constants in one place: `LLM_TIMEOUT_SECONDS = 30.0`, `GITHUB_TIMEOUT_SECONDS = 10.0`, `DB_COMMAND_TIMEOUT_SECONDS = 10.0`, `REDIS_TIMEOUT_SECONDS = 5.0`.
- `backend/reliability/guard.py`:
  - `class Guard` — composes breaker + retry + timeout in the correct order. `__init__(self, *, breaker: CircuitBreaker | None = None, attempts=3, base_delay=0.1, timeout_seconds: float | None = None, retry_on=(Exception,))`. `async def __call__(self, op, *, name="")` → `breaker.before_call()`; then `retry_async(lambda: with_timeout(op(), timeout_seconds) if timeout_seconds else op(), attempts=…, retry_on=…)`; on any escaping exception `breaker.on_failure()` + re-raise, else `breaker.on_success()`. Pure, no project imports.
- `backend/reliability/idempotency.py`:
  - `def fingerprint(*parts: object) -> str` — stable `sha256` hex of `\x1f`-joined `repr`-normalised parts. Used to content-address a finding for the dedup index / assertions.
  - `class OnceGuard` — in-process `set[str]` "have I already applied this key" helper for retry-safe side effects where the DB constraint isn't the dedup point (documented; used by the fault-injection test to demonstrate the pattern).
- `backend/reliability/conformance.py` (test helper, importable):
  - `BreakerLike` Protocol (`before_call`, `on_success`, `on_failure`) + `assert_breaker_contract(make_breaker)` — drives any breaker through closed→open→half-open→closed and asserts the shared behaviour. Used by `test_reliability_fault_injection.py` against `reliability.CircuitBreaker`, `job_queue.queue.CircuitBreaker`, `integrations.github_client._CircuitBreaker`.
- **Wiring (no behaviour change beyond adding resilience):**
  - `integrations/truth_store.py` — `TruthStore.__init__(pool, *, guard: Guard | None = None)` → `self._guard = guard or Guard(breaker=CircuitBreaker(name="tiger-truth"), timeout_seconds=DB_COMMAND_TIMEOUT_SECONDS, retry_on=(asyncpg.PostgresConnectionError, asyncpg.InterfaceError, OSError))`. Each public method routes its `pool.execute/fetch/executemany` through `await self._guard(lambda: …, name="truth_store.<m>")`. `insert_findings` SQL gets `ON CONFLICT DO NOTHING` (idempotent re-insert).
  - `economics/cost_repository.py` — same `guard` param + wrap `spend_today_usd`. Drop the "retry/breaker deferred" comment.
  - `memory/context_retriever.py` — wrap the **DB** call in `retrieve()` (the FTS+vector query) with a `Guard`; the `embed()` OpenAI call is covered by the LLM breaker below. `DB_COMMAND_TIMEOUT_SECONDS` now imported from `reliability.timeout` (single source).
  - `agents/llm_client.py` — add `async def guarded_parse(llm, *, breaker: CircuitBreaker | None = None, **kwargs)` — module-level default breaker `_LLM_BREAKER = CircuitBreaker(name="openai", failure_threshold=5)`; wraps `llm.chat.completions.parse(**kwargs)` in `Guard(breaker=…, attempts=1, timeout_seconds=LLM_TIMEOUT_SECONDS)` (attempts=1 — the OpenAI SDK already retries internally; the breaker is the new layer). `create_llm_client` unchanged.
  - `agents/base_agent.py` — `_parse_completion` calls `guarded_parse(self._llm, …)` instead of `self._llm.chat.completions.parse(…)`. One line.
  - `evaluation/judge.py` — same one-line swap to `guarded_parse` (keeps the judge on the shared breaker; still non-blocking for the gate).
- `scripts/migrations/2026-09-reliability.sql` — idempotent: `CREATE UNIQUE INDEX IF NOT EXISTS finding_records_dedup_idx ON finding_records (review_id, agent_type, file_path, COALESCE(line_start, -1), category);` (COALESCE so NULL lines still dedup). Comment explains it makes `insert_findings` retry-safe.
- `backend/tests/test_reliability_fault_injection.py` (fake-driven, no creds):
  - `test_retry_then_succeeds` — op fails twice then returns; `retry_async` returns the value; call count == 3.
  - `test_retry_exhausted_reraises_last` — op always raises `Boom`; after `attempts` tries the last `Boom` propagates; count == attempts.
  - `test_non_retryable_exception_not_retried` — `retry_on=(KeyError,)`, op raises `ValueError` → propagates immediately, count == 1.
  - `test_timeout_surfaces_as_operation_timeout` — a `sleep(10)` op under `with_timeout(…, 0.01)` → `OperationTimeout`.
  - `test_breaker_opens_and_fast_fails` — `Guard` with `failure_threshold=2`; two failing calls, third raises `CircuitOpenError` and the op is **not invoked** (spy count frozen at 2).
  - `test_breaker_half_opens_after_cooldown` — `reset_after_seconds=0` → next call probes (reaches op), success closes it.
  - `test_guard_records_success_resets_failures` — fail once, succeed, fail once → breaker still closed (non-consecutive).
  - `test_retried_db_write_has_no_duplicate_effect` — a fake pool whose `executemany` raises `InterfaceError` on the 1st attempt, records rows on the 2nd; assert the fake ends with exactly one copy of each finding (retry + `ON CONFLICT DO NOTHING` semantics, simulated) — **the key idempotency assertion**.
  - `test_llm_breaker_trips_after_repeated_failures` — `guarded_parse` with a fake `llm` that always raises a 5xx-like error; after `failure_threshold` calls the next raises `CircuitOpenError` without calling the fake.
  - `test_all_breakers_satisfy_the_contract` — `assert_breaker_contract` against `reliability.CircuitBreaker`, `job_queue.queue.CircuitBreaker`, `integrations.github_client._CircuitBreaker`.
  - `test_finding_dedup_index_regression` *(live, skipif on `TIGER_DATABASE_URL`)* — insert a review + findings twice via `TruthStore.insert_findings`; `SELECT count(*)` shows one set, not two; clean up.
- Update existing tests only where a constructor signature changed (`TruthStore`, `CostRepository` gain an optional `guard=` kwarg — default path unchanged, so existing call sites keep working; add targeted assertions if trivial).

**Demo command:** `pytest backend/tests/test_reliability_fault_injection.py -k "retry_then_succeeds or breaker_opens_and_fast_fails or retried_db_write_has_no_duplicate_effect"`

**Success criteria:** the 3 demo tests pass; `retry_async` retries only `retry_on` types and re-raises the last; `with_timeout` raises `OperationTimeout`; `Guard`'s breaker opens after the threshold and fast-fails without invoking the op, half-opens after cooldown; a retried DB write leaves exactly one copy of each finding; the LLM breaker trips after repeated failures; all three existing/near breakers pass one shared conformance contract; `mypy backend/reliability backend/integrations backend/economics backend/agents backend/memory` clean; `pytest -q` green (full suite, live bits skip without creds).

**Invariant check:** `outbound_call_safety` — this milestone closes it for the DB + LLM surface (Redis + GitHub already had it); documented residuals: `SELECT … FOR UPDATE` in `upsert_review` and the `mark_posted` crash window (need the not-yet-built retrying ARQ caller). `dependency_direction` — `reliability/` is a pure leaf (zero project imports), every consumer imports *inward*; no new cross-cutting exception. `structured_output_only` / `single_data_spine` — not implicated (breaker state is in-process, ephemeral).

**Out of M10 scope (carried forward):** DRYing `job_queue` + `github_client` onto the shared `reliability/` module (left as-is, tested, conformance-checked); `SELECT … FOR UPDATE` + `mark_posted` crash-window hardening; a real chaos/live fault-injection harness against Tiger/Redis; predictive vs reactive breaker tuning; wiring breaker state onto the `agent_events` observability spine; DX / prompt playground / trace viewer (phase 17); CI/CD eval-gate wiring (phase 18); drift detection (phase 20); the unchanged M5/M6 graph-rewiring + ARQ `run_review` worker debt.

## iter 1 · 2026-09-08
- gate G1: pass · skills considered [genesis LOOPS canon, coding-orchestrator → production-readiness, tdd]; reliability/ as a pure leaf, migrate DB + LLM surface, leave the two existing breakers + conformance-test them.
- gate G2: pass · new: `backend/reliability/{__init__,circuit_breaker,retry,timeout,guard,idempotency,conformance}.py` + `backend/tests/test_reliability_fault_injection.py` (16 tests) + `scripts/migrations/2026-09-reliability.sql` (+ folded the index into `2026-06-tiger-init.sql` for fresh provisions). Wired: `truth_store.py` (Guard on every write, `insert_findings` → `ON CONFLICT DO NOTHING`), `cost_repository.py` (Guard, dropped the "deferred" comment), `context_retriever.py` (Guard on the hybrid query + a breaker on `embed()`; `DB_COMMAND_TIMEOUT_SECONDS` now sourced from `reliability.timeout`), `llm_client.py` (`guarded_parse()` + process-wide `openai-chat` breaker), `base_agent._parse_completion` + `evaluation/judge.py` (one-line swap to `guarded_parse`).
- gate G3: pass · ~34k this milestone.
- gate G4: pass · `uv run mypy .` → only the 3 pre-existing M3 `test_orchestrator.py` errors (71 files); `mypy reliability integrations economics agents memory evaluation` clean. `uv run pytest -q` → **84 passed** (68 prior + 16 new), 164s (live e2e + the new live dedup regression ran — `.env` creds present). Applied `2026-09-reliability.sql` to live Tiger (index `finding_records_dedup_idx` confirmed). Demo `pytest tests/test_reliability_fault_injection.py -k "retry_then_succeeds or breaker_opens_and_fast_fails or retried_db_write_has_no_duplicate_effect"` → **3 passed, 13 deselected**.
- gate G5: deferred to L4.
- decisions:
  - `reliability/` is a **pure leaf** — zero imports under `backend/`, sits at the `core/` tier; every consumer imports inward, so `dependency_direction` holds with **no new cross-cutting exception** (unlike `observability/` M6 and `security/` M8). The context-graph invariant text was left unchanged deliberately.
  - `Guard` composes breaker → retry → timeout in that fixed order; one `Guard` (one breaker) per downstream dependency, reused for every call to it. DB guards retry only transient connection errors (`PostgresConnectionError`, `InterfaceError`, `OperationTimeout`, `ConnectionError`, `OSError`) — a bad-SQL / constraint error fails immediately.
  - LLM guards use `attempts=1` — the OpenAI SDK already retries with backoff internally; the new layer is the breaker + an outer `asyncio.wait_for` timeout. One process-wide `openai-chat` breaker so a GPT outage trips once for all four specialists + the judge, not per-agent.
  - `job_queue.CircuitBreaker` + `github_client._CircuitBreaker` left in place (working, independently tested); `conformance.assert_breaker_contract` pins all three (+ `reliability.CircuitBreaker`) to one behaviour — closed→open at threshold→fast-fail→half-open after cooldown→close on probe. A later milestone can DRY them.
  - idempotency: `finding_records_dedup_idx` UNIQUE `(review_id, agent_type, file_path, COALESCE(line_start,-1), category)` + `ON CONFLICT DO NOTHING` makes `insert_findings` retry-safe. `SELECT … FOR UPDATE` in `upsert_review` and the `mark_posted` crash window deferred (need the not-yet-built retrying ARQ caller) — documented, not fixed blind.
  - demo `-k` uses `or` across 3 distinct test names (a lesson from M8 — `and` selects nothing).
- next: L4 VERIFY (fresh checker), then quiz-me.

## L4 VERIFY · 2026-09-08
- checker: fresh general-purpose agent, no build trail. Ran all commands independently from `backend/`.
- **verdict: APPROVE**
- SC1–SC9 all PASS: `retry_async` retries only `retry_on`, exp backoff, re-raises last (non-retryable propagates first raise); breaker state is time-based, `on_success` clears `_opened_at`, probe failure re-opens; `Guard` composes `before_call` (outside the try — so a `CircuitOpenError` doesn't self-record a failure) → `retry_async(with_timeout(op))` → success/failure; `pytest tests/test_reliability_fault_injection.py` **16 passed** (live dedup regression ran); demo `-k "…or…or…"` 3 passed / 13 deselected; full suite **84 passed / 161s**; `mypy .` only the 3 known M3 `test_orchestrator.py` errors (71 files); dedup index + `ON CONFLICT DO NOTHING` proven non-vacuous by the live `test_finding_dedup_index_regression` (inserts 2 findings twice via the real `TruthStore` → `count(*)==2`); `assert_breaker_contract` non-trivial, all 3 breaker impls conform.
- invariants: `dependency_direction` HELD — `reliability/` imports only stdlib + intra-package, zero `backend/` deps, every consumer imports inward, the "no new cross-cutting exception" claim is legitimate (pure primitives, unlike `observability`/M6 + `security`/M8). `outbound_call_safety` CLOSED for the DB+LLM surface — truth_store (all 4 writes), cost_repository (both reads), context_retriever (hybrid query + `embed()`), `base_agent._parse_completion` + `judge` all guarded; no un-guarded asyncpg/openai calls left in those files. `structured_output_only` / `single_data_spine` untouched (breaker state in-process only). `_HYBRID_QUERY` SQL character-identical to the original; `guarded_parse` swap preserves all kwargs; `2026-06-tiger-init.sql` fold-in stays `IF NOT EXISTS`.
- non-blocking findings:
  1. `guard.py` `except BaseException` recorded a breaker failure on `asyncio.CancelledError`. **FIXED** — cancellation is now re-raised before `on_failure()`; new `test_cancellation_does_not_count_as_breaker_failure`.
  2. `test_retried_db_write_has_no_duplicate_effect` simulates `ON CONFLICT DO NOTHING` in its fake pool, so the dedup guarantee without creds rests on the live regression test. **ACCEPTED** — creds present in this env; the live test is the real proof.
  3. `insert_findings` used a bare `ON CONFLICT DO NOTHING` (no target). **FIXED** — now names the conflict target `(review_id, agent_type, file_path, COALESCE(line_start, -1), category)`, matching the index; live test still green (expression conflict target works against Tiger).
  4. `api/reviews.py` DB read is still un-guarded (documented in that file's own docstring as an internal read-only endpoint, pool has `command_timeout`). **ACCEPTED / carried forward** — added to the residual list below.
  5. Half-open allows unlimited concurrent probes in all 3 breakers (pre-existing, not introduced by M10). Carried forward.
- post-fix recheck: `mypy reliability integrations tests/test_reliability_fault_injection.py` clean; `pytest tests/test_reliability_fault_injection.py` **17 passed**.
- **carried forward (add to residuals):** `api/reviews.py` + `api/app.py` DB reads not yet `Guard`-wrapped; unbounded half-open probe concurrency in all breaker impls.
- quiz-me: PENDING.

## quiz-me · 2026-09-08
- **Q1 — why weren't `job_queue` / `github_client` refactored onto `reliability/`, and what stops drift?** → user answered *"they can't import `reliability/` — it would be a sideways dependency."* **CORRECTED.** That's not right: `reliability/` is a pure leaf at the `core/` tier, so `job_queue → reliability` and `integrations → reliability` are *inward* imports and perfectly legal — in fact `truth_store` / `cost_repository` / `context_retriever` import it exactly that way in this milestone. The real reason is **scope/risk control**: the two existing breakers already work and are independently tested, so rewiring them now buys nothing and risks a regression. `conformance.assert_breaker_contract` drives all three (+ `reliability.CircuitBreaker`) through the same closed→open→half-open→reset sequence, so any behavioural divergence is caught by a test — a later milestone can DRY them safely.
- **Q2 — why no `dependency_direction` cross-cutting exception (when M6/M8 needed one)?** → *"`reliability/` imports nothing under `backend/` — pure leaf at the `core/` tier, every consumer imports inward."* ✓ correct. `observability`/`security` had to reach into `agents/` internals (an outward `agents → observability` edge → needs an exception); `agents → reliability` is inward and legal.
- **Q3 — why `attempts=1` for the LLM guard vs `attempts=3` for DB?** → *"the OpenAI SDK already retries with backoff internally; the guard adds only the breaker + outer timeout."* ✓ correct. `create_llm_client` sets `max_retries=3` on the SDK; stacking guard retries would compound to 3×3. asyncpg has no built-in retry, so the DB guard supplies it.
- **result: 2/3 correct; Q1 misconception corrected above (structural-prohibition vs the actual scope-control rationale). Q2/Q3 show solid understanding of the leaf-module and retry-layering design — proceeding.**
- **M10 DONE.** Spec §4.1 phase 12 gate ("retries, circuit breakers, idempotency verified under fault injection") satisfied; `outbound_call_safety` invariant closed for the DB + LLM surface. Next: M11 (CI/CD eval gates / DX / continuous learning / governance — to be sliced).
