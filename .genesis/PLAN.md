# PLAN — ai-pr-review

The machine-parseable implementation plan. Mirrors the milestone table in `DONE.html` (DONE.html is the
human/visual view; this is the one loops read). Sliced so each milestone ships in one L1 BUILD pass.

> Slicing rule: a milestone must have (a) a single clear outcome, (b) an exact **demo command** that
> proves it, and (c) a freeze boundary of files it may touch. If you can't write the demo command,
> the milestone is too vague — split it.

---

## Brainstorm (G0.5 — fill before slicing milestones)

> Three fundamentally different approaches to the cognitive job. Pick one. Record the rationale.
> This is the cheapest design decision — you haven't written a line of code yet.

### Approach A — Single-LLM review
One prompt judges the whole diff in one pass.
- Strengths: Trivial to build, cheap, fast to demo.
- Weaknesses: One mindset collapses four distinct concerns (security/quality/tests/docs); no codebase grounding; hallucinates with confidence; no audit trail; no selectivity.

### Approach B — Static analysis + rules engine
Deterministic linters and data-flow analysis, no LLM.
- Strengths: Zero hallucination, fully reproducible, cheap to run.
- Weaknesses: No semantics — cannot judge intent, logic correctness, or whether a test is meaningful; high false-positive rate; cannot read documentation intent.

### Approach C — Agentic fan-out with grounded specialists (chosen)
Four parallel specialist agents (security, quality, tests, docs), each grounded via hybrid RAG retrieval over the codebase, merged by an aggregator that applies a confidence-weighted HITL gate before posting.
- Strengths: Each concern reasoned deeply, not shallowly; grounding removes the dominant hallucination failure mode; structured Finding contract makes results auditable and disputable; selective posture (L0) — surfaces only what's worth a senior's attention.
- Weaknesses: Requires orchestration (LangGraph), a retrieval layer, and a proof/observability layer — real infrastructure, not a single prompt.

### Chosen: Approach C — Agentic fan-out. Derived first-principles in `ai-pr-review-agent.html` (L0–L9): grounding (L4) is what separates this from a single-LLM reviewer, and the four concerns (L1) mirror how a senior engineer actually reviews. This is the only approach that supports the confidence-weighted HITL gate (L7) and the audit/proof requirement (L6).

---

## Milestones

> Reordered 2026-08-22 per user request: data infra first (independent of everything else), then
> ingestion, then orchestration/agents, with the frontend deliberately minimal and pushed late — a
> single status page, not a build focus. Each milestone lists exactly which credentials it needs so
> you can get each one only when you actually reach it. See the credentials walkthrough in
> `.genesis/checkpoints/CREDENTIALS.md`.

### M1 — Tiger Cloud data spine provisioned
- **Outcome:** Tiger Cloud service is live; `2026-06-tiger-init.sql` applied; the three lanes exist — `code_chunks` (pgvector/pgvectorscale/DiskANN + FTS), `agent_events` hypertable, and the continuous aggregates (`agent_health_1m`, `pr_cost_hourly`).
- **Phase (swe-master):** Phase 13/14 — Infrastructure & Data Engineering (Tiger integration Phase A, ADR-003)
- **Files / freeze boundary:** `backend/database/**`, `scripts/migrations/2026-06-tiger-init.sql`
- **Credentials needed:** `TIGER_DATABASE_URL` only.
- **Demo command:** `psql "$TIGER_DATABASE_URL" -c "\dx" | grep -E "timescaledb|vector|vectorscale" && psql "$TIGER_DATABASE_URL" -c "SELECT hypertable_name FROM timescaledb_information.hypertables;" | grep agent_events`
- **Success criteria:** All three extensions listed; `agent_events` reports as a hypertable; both continuous aggregate views exist in `information_schema.views`.
- **Loops:** L1, L4
- **Skills:** canon + tdd + data-systems-engineering
- **Token budget:** 50000

### M2 — Webhook ingress + queue
- **Outcome:** FastAPI receives a GitHub `pull_request` webhook, verifies its HMAC-SHA256 signature, dedups on the `X-GitHub-Delivery` idempotency key, and enqueues a review job to Redis/ARQ — returning 200 before any review work happens.
- **Phase:** Phase 3 — Backend & API
- **Files:** `backend/webhook_receiver/**`, `backend/job_queue/**`, `backend/api/schemas.py`
- **Credentials needed:** `REDIS_URL`, `GITHUB_APP_ID`, `GITHUB_WEBHOOK_SECRET`, `GITHUB_PRIVATE_KEY_PATH`, and a smee.io URL for local webhook delivery.
- **Demo command:** `pytest backend/tests/test_webhook_ingress.py -k "valid_signature and idempotent_replay and enqueues_job"`
- **Success criteria:** Valid signed payload → 200 + one job in the ARQ queue. Replayed delivery ID → 200 + no second job. Bad signature → 401, nothing enqueued.
- **Loops:** L1, L4
- **Skills:** canon + tdd + data-systems-engineering
- **Token budget:** 50000

### M3 — LangGraph orchestrator: parallel fan-out + checkpointing
- **Outcome:** `core/workflow_engine.py` interface + `orchestrator/langgraph_engine.py` implementation. A StateGraph fans out to four stub specialist nodes via the Send API, joins at a stub aggregator, checkpoints state to Redis at each node boundary, and resumes correctly after a simulated mid-run crash.
- **Phase:** Phase 4 — Workflow Orchestration
- **Files:** `backend/core/workflow_engine.py`, `backend/orchestrator/**`
- **Credentials needed:** none beyond M1/M2 (`TIGER_DATABASE_URL`, `REDIS_URL`) — the 4 nodes are stubs here, no LLM calls yet.
- **Demo command:** `pytest backend/tests/test_orchestrator.py -k "parallel_fanout and checkpoint_resume_after_crash"`
- **Success criteria:** All 4 stub nodes observed running concurrently (overlapping start/end timestamps in test); killing the worker after 2/4 nodes complete and resuming reaches the aggregator without re-running the completed 2.
- **Loops:** L1, L4
- **Skills:** canon + tdd + distributed-systems
- **Token budget:** 50000

### M4 — Grounded specialist agents + Finding contract
- **Outcome:** `agents/contracts.py` (Finding schema) + `agents/base_agent.py` + the four specialists (security, quality, tests, docs). Each queries `memory/context_retriever.py` (hybrid DiskANN + FTS over `code_chunks`) and returns a list of schema-valid `Finding` objects for a real diff.
- **Phase:** Phase 5/6/8 — LLM & Reasoning, Memory Architecture, Multi-Agent Systems
- **Files:** `backend/agents/**`, `backend/memory/**`, `backend/prompts/**`
- **Credentials needed:** `OPENAI_API_KEY` (embeddings + LLM calls), plus M1's `TIGER_DATABASE_URL`.
- **Demo command:** `pytest backend/tests/test_specialists_e2e.py -k "seeded_repo_diff_produces_valid_findings"`
- **Success criteria:** Against a seeded `code_chunks` fixture repo and a known-vulnerable diff fixture, all 4 agents return ≥1 `Finding` each, every `Finding` validates against the Pydantic schema, and the security agent's findings include the seeded SQL-injection line.
- **Loops:** L1, L3 (research), L4
- **Skills:** canon + tdd + llmops-ai-agents
- **Token budget:** 50000

### M5 — Aggregator, HITL gate, GitHub posting
- **Outcome:** `orchestrator/nodes.py:aggregate` merges the four Finding lists, dedups same file/line findings across agents, computes `overall_confidence`, and applies the confidence-weighted HITL gate (L7): auto-post via `integrations/github_client.py` when confident and CRITICAL-free, else insert into `hitl/queue.py`.
- **Phase:** Phase 3/7/19 — Backend & API, Tooling, Human-in-the-Loop
- **Files:** `backend/orchestrator/nodes.py`, `backend/hitl/**`, `backend/integrations/**`
- **Credentials needed:** none new — reuses M2's GitHub App credentials to post.
- **Demo command:** `pytest backend/tests/test_aggregator_hitl_e2e.py -k "high_confidence_auto_posts and low_confidence_queues and critical_always_escalates"`
- **Success criteria:** High-confidence/no-CRITICAL fixture → `github_client.post_review` called once, nothing in HITL queue. Low-confidence fixture → no post, one row in `hitl_reviews`. Any-CRITICAL fixture → no post regardless of confidence, escalation row written.
- **Loops:** L1, L2, L4
- **Skills:** canon + tdd + production-readiness
- **Token budget:** 50000

### M6 — Observability spine + cost ledger + BudgetGuard
- **Outcome:** `observability/events.py:emit_agent_event` wired into every orchestrator node and the LLM client, landing rows in `agent_events`. `economics/budget.py` BudgetGuard reads `agent_health_1m` at the top of every agent run and hard-blocks before the next LLM call if the daily cap is exceeded (ADR-004).
- **Phase:** Phase 10/16 — Observability & Tracing, Economics & Cost Control (Tiger integration Phase B)
- **Files:** `backend/observability/**`, `backend/economics/**`
- **Credentials needed:** none new.
- **Demo command:** `pytest backend/tests/test_observability_budget_e2e.py -k "full_review_traces_to_agent_events and budget_guard_blocks_over_cap"` `&& psql "$TIGER_DATABASE_URL" -c "SELECT count(*) FROM agent_events WHERE review_id = :last_test_review_id;"`
- **Success criteria:** Running M5's pipeline produces span.start/llm.call/tool.call/span.end/decision rows in `agent_events` for all 4 agents + aggregator, queryable by `review_id`. Setting the daily cap below the cost of one call causes the next agent run to raise `BudgetExceeded` before any LLM call fires.
- **Loops:** L1, L4
- **Skills:** canon + tdd + llmops-ai-agents
- **Token budget:** 50000

### M7 — Minimal status frontend (deliberately small — do not over-invest here)
- **Outcome:** One Next.js page, no design system, no polish pass. It reads the M6 aggregates and lists recent reviews with cost/confidence/status. Exists so there's something to look at, not as a build focus.
- **Phase:** Phase 2 — Frontend Engineering (kept intentionally thin)
- **Files:** `frontend/src/app/page.tsx`, `frontend/src/lib/api.ts` only — no component library, no auth UI, no design-system skill invoked.
- **Credentials needed:** none new — reads the same backend API M5/M6 already expose.
- **Demo command:** `curl -s http://localhost:3000 | grep -q "Reviews"`
- **Success criteria:** Page renders a table of reviews (id, status, cost, confidence) fetched from `/api/reviews`. That's it — no styling gate, no responsive-design gate.
- **Loops:** L1, L4
- **Skills:** canon + tdd only — explicitly skip the design-system skill for this milestone
- **Token budget:** 20000 — capped deliberately lower than the other milestones

### M8 — Security: threat model + prompt-injection guard
- **Outcome:** `backend/security/threat_model.md` (written threat model — assets, trust boundaries, prompt-injection via untrusted diff/file content, mitigations, deferred items) + `backend/security/injection_guard.py` (`InjectionGuard` — structural/deterministic: wraps untrusted diff text + each retrieved chunk in unique sentinel delimiters, contributes a system-prompt hardening clause, applies a known-pattern denylist for instruction-override / role-spoofing / fence-breakout / zero-width chars, returns a `GuardResult` noting what it flagged). Wired into `agents/base_agent.py::_build_prompt` behind a local `SupportsPromptGuard` Protocol, **on by default** (fail-safe-on, not opt-in). Closes `DONE.html` §2 DoD gate #4.
- **Phase:** Phase 11 — Security (threat model; prompt injection / untrusted input). RBAC + secret-masking deliberately deferred (no auth surface yet; GitHub App key already isolated in `backend/secrets/`).
- **Files:** `backend/security/**` (new), `backend/agents/base_agent.py` (wiring only — freeze boundary widened, user-approved 2026-09-07), `backend/tests/test_injection_guard.py` (new).
- **Credentials needed:** none new.
- **Demo command:** `pytest backend/tests/test_injection_guard.py -k "adversarial_diff_is_neutralized or benign_diff_unchanged or guarded_prompt_reaches_specialist"`
- **Success criteria:** A diff carrying instruction-override / role-spoof / fence-breakout payloads is delimited + flagged by the guard; the specialist `user` message contains the sentinels and the hardening clause and never the raw payload as free instruction text. A benign diff passes through semantically unchanged (same code content, only wrapped). The full specialist pipeline still produces a schema-valid `SpecialistReviewDraft` with the guard in the path. `mypy backend/security backend/agents` clean; `pytest -q` green.
- **Loops:** L1, L4
- **Skills:** canon + tdd + security-engineering (via coding-orchestrator route)
- **Token budget:** 50000

### M9 — Evaluation: golden dataset + LLM-as-judge + regression gate
- **Outcome:** `backend/evaluation/` (new leaf module) — `golden_dataset.py` (`GoldenCase`/`ExpectedFinding` + `load_golden_cases()` over `golden/*.yaml`, 3 cases: SQL-injection diff, missing-tests-and-docs, benign-refactor false-positive guard), `scorer.py` (deterministic `matches()` + `score_case()`/`score_suite()` → recall + false-positive count vs `expect_max_findings`), `judge.py` (`LLMJudge` — non-blocking rationale-quality score, reuses the M4 `llm_client`), `regression_gate.py` (`evaluate(suite, baseline, thresholds) -> GateReport` pure fn over a committed `baseline.json`; `python -m evaluation.regression_gate` CLI exits non-zero on regression), `runner.py` (`run_suite()` — live: seeds `code_chunks`, runs the 4 real specialists, scores, always cleans up). Proves spec §4.1 phase 9: "Golden dataset runs; LLM-as-judge scores; regression gate blocks."
- **Phase:** Phase 9 — Evaluation. Reliability-under-fault-injection → M10; DX / CI-CD eval-gate wiring / drift detection deliberately deferred (phases 17/18/20).
- **Files:** `backend/evaluation/**` (new), `backend/tests/test_evaluation.py` (new), `backend/pyproject.toml` (`pyyaml` → `dev` group). No production module touched — `evaluation/` is test/CI-only, nothing imports it.
- **Credentials needed:** none new — the live golden run reuses `OPENAI_API_KEY` + `TIGER_DATABASE_URL` (same skipif contract as `test_specialists_e2e.py`); deterministic scorer/gate tests need no creds.
- **Demo command:** `pytest backend/tests/test_evaluation.py -k "golden_recall_computed or regression_gate_blocks_on_drop or regression_gate_passes_at_baseline"`
- **Success criteria:** `load_golden_cases()` reads 3 YAML cases; `score_case()` computes recall + false-positive count on synthetic `Finding` lists; `evaluate()` returns `passed False` on a recall drop past tolerance and on a new false positive, `passed True` at baseline; the golden-run e2e produces a `SuiteScore` against the real specialists (skips cleanly without creds) and the LLM-judge scores matched findings. `mypy backend/evaluation` clean; `pytest -q` green.
- **Loops:** L1, L4
- **Skills:** canon + tdd + llmops-ai-agents (via coding-orchestrator route)
- **Token budget:** 50000

<!-- M10+ (reliability under fault injection — phase 12; then CI/CD eval gates phase 18,
     DX / prompt playground phase 17, continuous learning phase 20) — slice after M9 lands,
     per ai-pr-review-agent.html §4.1 -->

---

## Progress (loops append here on milestone completion — newest last)

- 2026-08-22 · **M1 DONE** — Tiger Cloud data spine provisioned. Migration `scripts/migrations/2026-06-tiger-init.sql` applied against the live service: `code_chunks` (pgvector/pgvectorscale/DiskANN + FTS), `agent_events` hypertable, `agent_health_1m` + `pr_cost_hourly` continuous aggregates, plus the truth-lane tables. L4 VERIFY (fresh agent, no build trail): APPROVE, one fix applied (explicit `timescaledb_toolkit` extension, previously implicit). Quiz-me: 2/3 correct — DiskANN-vs-HNSW rationale corrected in `checkpoints/M1.md`. Next: M2.
- 2026-09-04 · **M2 DONE** — Webhook ingress + queue. `backend/webhook_receiver/` (FastAPI `/webhook`, HMAC-SHA256 signature verification, `X-GitHub-Delivery` dedup) + `backend/job_queue/` (ARQ enqueue, Redis dedup claim/rollback, retry-with-backoff + circuit breaker) + `backend/api/schemas.py`. Also fixed two pre-existing `backend/.env` bugs (corrupted `REDIS_URL`, wrong `GITHUB_PRIVATE_KEY_PATH`). L4 VERIFY round 1: REJECT (missing retry-with-backoff, connect-timeout-only, job-loss race on failed enqueue after dedup claim). All 4 required fixes applied + regression test added. L4 VERIFY round 2: APPROVE. Quiz-me: Q1 corrected (dedup-ordering is about idempotency-on-retry, not GitHub-timeout avoidance), Q2/Q3 confirmed understood. 13/13 tests green, mypy clean, demo command passes verbatim. Next: M3.
- 2026-09-06 · **M4 DONE** — Grounded specialist agents + Finding contract. `backend/agents/{contracts,base_agent,llm_client}.py` + 4 specialists (`security/quality/tests/docs`) + `backend/prompts/*.md` + `backend/memory/context_retriever.py` (hybrid DiskANN vector + Postgres FTS over `code_chunks`, combined by RRF) + `backend/tests/{test_base_agent,test_specialists_e2e}.py`. G0 re-check: the 2026-09-04 session had drafted the code but never checkpointed/verified it, and it was non-working — it imported `openai`, which was never installed or in `pyproject.toml` (`anthropic`+`voyageai` were declared instead). User decision: stay on OpenAI (plan updated), keep `anthropic`+`voyageai` reserved for later. Integration-only fixes: added `openai>=1.40.0`, fixed an event-loop-scope bug in the live e2e test (module-scoped asyncpg pool vs function-scoped loop → `loop_scope="module"`), one `type: ignore` on `asyncpg`. No specialist/retriever/contract logic touched. L4 VERIFY round 1: APPROVE — 26/26 tests, mypy clean on M4 dirs, demo `pytest -k seeded_repo_diff_produces_valid_findings` green against live OpenAI + Tiger Cloud (all 4 agents ≥1 `Finding`, security agent flags the seeded SQL injection). Invariants `structured_output_only` / `single_data_spine` / `dependency_direction` PASS; `outbound_call_safety` partial-acceptable (circuit breaker deferred to L8/L12 per the invariant's own text). Quiz-me: Q1 (two-tier `FindingDraft`→`Finding` split) clarified — user had answered with the maker/checker rationale; Q2/Q3 confirmed. Carry-forward noted in `checkpoints/M4.md`. Next: M5.
- 2026-09-07 · **M6 DONE** — Observability spine + cost ledger + BudgetGuard. `backend/observability/events.py` (`AgentEventSink` → one append-only `INSERT` per action into the `agent_events` hypertable: `span.start`/`tool.call`/`llm.call`/`span.end`/`decision`; auto-stamps `cost_usd` on `llm.call` from `economics.pricing`; `NullEventSink` no-op for opt-out) + `observability/tracing.py` (`get_trace(pool, review_id)` = `WHERE review_id=$1 ORDER BY ts` + `trace_summary`) + `economics/pricing.py` (approx OpenAI per-1M-token map + `cost_usd()`) + `economics/cost_repository.py` (`spend_today_usd()` — raw `SUM(cost_usd)` over the UTC day, explicitly TZ-pinned) + `economics/budget.py` (`BudgetGuard.check()` raises `BudgetExceeded` at `spend >= daily_cap`; `from_env` reads `DAILY_BUDGET_USD`). Wired opt-in (freeze boundary widened by user approval): `agents/base_agent.py` — `review(diff, *, review_id=None)`; with a `review_id` the budget guard runs first (before any span/LLM), then span/tool/llm/span events emit; `review_id=None` is the untouched M4 path. `agents/llm_client.py` — `usage_of()`. `orchestrator/nodes.py` — `aggregate(events=...)` emits one `decision` row. `dependency_direction` kept: `agents/` imports `observability` (documented cross-cutting exception) but not `economics` — `BudgetGuard` injected behind a local `SupportsBudgetCheck` Protocol (M5's trick). No new deps. L4 VERIFY (fresh checker, no build trail): APPROVE — independently ran `mypy observability economics orchestrator agents` clean (21 files), `pytest -q` **51 passed** (48 prior + 3 new e2e, not skipped), demo `pytest tests/test_observability_budget_e2e.py -k "full_review_traces_to_agent_events and budget_guard_blocks_over_cap"` green on live Tiger Cloud (real `agent_events` writes + trace read-back; fake LLM/retriever). SC1 (5 event types + 4 agents + aggregator by `review_id`) + SC2 (`check()` provably before `.parse()`, spy confirms no LLM call) + SC3 (mypy/pytest) all met. Invariants `structured_output_only` / `single_data_spine` / `dependency_direction` PASS; `outbound_call_safety` PASS (pool `command_timeout=10`; DB retry/breaker deferred to L8/L12). 2 non-blocking findings fixed (`spend_today_usd` UTC-day boundary was session-TZ-dependent → pinned to UTC; removed dead `span()` helper), 1 accepted (observability→economics.pricing layering). Quiz-me: Q1 (opt-in default + Protocol-injection-protects-`dependency_direction`) + Q2 (all 4 agents raise `BudgetExceeded`, nothing emitted — `check()` raises before `span.start`) corrected and confirmed; Q3 (raw-sum vs lagging continuous aggregate) correct. **Out of scope, carried forward:** `graph.py` rewiring to the real pipeline + ARQ `run_review` worker (unchanged from M5); `api/economics_router` + frontend economics page (Tiger Phase D — M7); `observability/audit.py`/`alerting.py`/OTel export (roadmap Phase 10); predictive (vs post-hoc) budget blocking; a short-window cost rollup/cache for BudgetGuard at high throughput; a "blocked" `agent_events` row when the guard trips. Next: M7.
- 2026-09-07 · **M5 DONE** — Aggregator, HITL gate, GitHub posting. `backend/orchestrator/nodes.py` (`dedup_findings` by `(file,line)` keeping highest confidence + agreement note, `overall_confidence` = mean / 1.0-when-empty, `Decision` enum + `decide()` — any CRITICAL → ESCALATED, else score < 0.75 → QUEUED, else AUTO_POST; `aggregate()` orchestrates dedup→score→`upsert_review`→`insert_findings`→branch post/queue; `_render_review_body` markdown grouped by severity) + `backend/integrations/{github_client,truth_store}.py` (GitHubClient: RS256 App-JWT, per-repo installation-id cache via `GET /repos/{owner}/{repo}/installation`, installation-token cache, `post_review`, self-contained timeout+retry+circuit-breaker; TruthStore: asyncpg, idempotent `upsert_review` on `github_delivery_id` UNIQUE, `insert_findings`, `mark_posted`, `set_status`) + `backend/hitl/queue.py` (`HitlQueue.enqueue`) + tests `test_aggregator_nodes.py` (17 pure) + `test_aggregator_hitl_e2e.py` (5 live-DB incl. demo). `+pyjwt[crypto]>=2.9`. `aggregate()` collaborators injected via local Protocols so `nodes.py` imports neither `integrations/` nor `hitl/` (preserves `dependency_direction`). L4 VERIFY (fresh checker, no build trail): APPROVE — 48/48 tests, mypy clean on `orchestrator/hitl/integrations`, demo `-k "high_confidence_auto_posts and low_confidence_queues and critical_always_escalates"` green on live Tiger Cloud (all 3 routing outcomes + exactly one GitHub call). Invariants `structured_output_only` / `single_data_spine` / `dependency_direction` PASS; `outbound_call_safety` PASS for GitHub client, DB-call retry/breaker deferred to L8/L12 per invariant text. Quiz-me: Q1 (Protocol injection = decoupling + dependency_direction) + Q2 (redelivery early-returns on `status != 'pending'`, no write, no post) correct; Q3 (`Finding`→`finding_records`: `title`→`summary`, `line`→`line_start`=`line_end`, `suggestion`→NULL; lossy for multi-line spans) answered after explanation. **Out of scope, carried forward:** rewiring `orchestrator/graph.py` to call `nodes.aggregate` + swap stub specialists for real M4 agents; the ARQ `run_review` worker; crash-window idempotency hardening (`mark_posted` gap, `finding_records` has no unique constraint) + `SELECT … FOR UPDATE` in `upsert_review` — must close when `aggregate()` gets a retrying caller. Pre-existing debt: 3 mypy errors in M3's `test_orchestrator.py` (runtime green). Next: M6.
- 2026-09-04 · **M3 DONE** — LangGraph orchestrator: parallel fan-out + checkpointing. `backend/core/workflow_engine.py` (ADR-001 `WorkflowEngine` Protocol) + `backend/orchestrator/graph.py` (4 stub specialists fanned out via the `Send` API, stub aggregator join) + `backend/orchestrator/redis_checkpointer.py` (hand-written minimal async `BaseCheckpointSaver` against plain Redis — skipped `langgraph-checkpoint-redis`, which needs a RediSearch module Upstash's free tier doesn't have) + `backend/orchestrator/langgraph_engine.py`. L4 VERIFY round 1: REJECT — resume passed `{}` instead of `None` as the graph input, causing LangGraph to re-run ALL 4 specialists on resume instead of just the incomplete ones; the test's own rerun-check was also structurally blind to this (set-difference on cumulative call history). Both fixed (resume input corrected; tests rewritten to slice the post-resume call tail) and confirmed non-flaky across 8 repeated runs. L4 VERIFY round 2: APPROVE — verifier independently reintroduced the bug to confirm both the fix and the regression tests are load-bearing. Quiz-me: Q1 corrected (`None` = resume/continue, not "already executed"; any non-`None` input = fresh dispatch from START), Q2/Q3 confirmed understood. 16/16 tests green, mypy clean, demo command passes verbatim. Next: M4.
- 2026-09-08 · **M9 DONE** — Evaluation: golden dataset + LLM-as-judge + regression gate. New leaf module `backend/evaluation/`: `golden_dataset.py` (`GoldenCase`/`ExpectedFinding`/`RepoFile` pydantic + `load_golden_cases()` over `golden/*.yaml`) + 3 YAML cases (`sql_injection_lookup` — f-string SQL injection, no test, no docstring, with a parameterized-query contrast in `repo_context`; `missing_tests_and_docs` — new `apply_discount` with `security: 0` cap; `benign_refactor` — pure local-var rename, `expected_findings: []` + `security:0/tests:0/docs:0/quality:1` caps = the false-positive guard). `scorer.py` — deterministic `matches()` (agent_type + file + line±tolerance + category/rationale substring) + `score_case()` (greedy 1:1, recall = matched/expected, 1.0 when expected==0; false_positives = Σ max(0, per-agent count − `expect_max_findings` cap)) + `score_suite()` → `SuiteScore` (`mean_recall`, `total_false_positives`). `judge.py` — `LLMJudge` scoring rationale quality 0–1 as a structured `RationaleScore`; **never on the blocking path** (imported only by the live e2e), reuses the shared M4 `AsyncOpenAI` client. `regression_gate.py` — pure `evaluate(suite, baseline, thresholds) -> GateReport` (fails on `recall_drop > max_recall_drop=0.05` OR `fp_increase > max_fp_increase=0`) + `load/write_baseline` over a committed `baseline.json` + `python -m evaluation.regression_gate [--update-baseline]` CLI (loads `.env` best-effort; exits 0 w/ skip notice when no creds, non-zero on regression). `runner.py` — `run_suite()`/`run_case()` seed each case's `repo_context` into `code_chunks` under a unique `eval-golden/{id}-{uuid}` repo and always `DELETE` in `finally` (mirrors the `seeded_repo` fixture). `tests/test_evaluation.py` — 10 tests (9 deterministic scorer/gate/loader, no creds; 1 live e2e skipif on `TIGER_DATABASE_URL`/`OPENAI_API_KEY`, `loop_scope="module"`). `pyproject.toml` `dev` += `pyyaml`/`types-pyyaml`. No `dependency_direction` change — `evaluation → agents` edge already declared, `evaluation/` is a leaf (nothing imports it). **Real baseline seeded from a live run:** `mean_recall=1.0`, `total_false_positives=0` across all 3 cases. L4 VERIFY (fresh checker, no build trail): APPROVE — SC1–SC8 all PASS (`load_golden_cases()` → 3; scorer/gate maths confirmed pure at `scorer.py:69-93` / `regression_gate.py:53-82`; `pytest tests/test_evaluation.py` 10 passed with the live e2e actually running; demo `-k "golden_recall_computed or regression_gate_blocks_on_drop or regression_gate_passes_at_baseline"` 3 passed / 7 deselected / 0.05s; `mypy evaluation tests/test_evaluation.py` clean; full suite green for M9 — the intermittent 2–3 errors are all pre-existing M6 `test_observability_budget_e2e.py` Tiger-Cloud connection drops, isolated rerun 13 passed). Invariants `dependency_direction` / `structured_output_only` / `single_data_spine` / `outbound_call_safety` all PASS. 5 non-blocking findings — 3 fixed (`runner` seed moved inside try/finally; a near-vacuous tolerance test rewritten to feed a real 0.875-vs-0.90 dip; dead import removed), 2 accepted (strict 1.0 baseline = deliberate gate sensitivity, `--update-baseline` re-seeds on intended model changes; judge wired only into the e2e not the CLI = non-blocking observability layer by design). Quiz-me: 3/3 correct (judge off the blocking path keeps CI deterministic + cheap; a missed SQL-injection finding drops mean recall ~0.11 and trips the gate; unique per-run repo + `DELETE` in `finally` isolates seeded `code_chunks`). **Out of scope, carried forward:** wiring the gate into real CI / GitHub Actions + canary path (phase 18); prompt playground + trace viewer (phase 17); drift detection over continuous aggregates (phase 20); reliability under fault injection (M10 / phase 12); persisting eval-run history to Tiger; expanding the golden set beyond 3 cases; an eval dashboard in the frontend; reporting `LLMJudge` scores from the CLI; plus the unchanged M5/M6 graph-rewiring + ARQ `run_review` worker debt and the still-unwired observability hooks. Next: M10.
- 2026-09-08 · **M8 DONE** — Security: threat model + prompt-injection guard. `backend/security/threat_model.md` (assets: GitHub write token / review verdict / cost budget; 5 trust boundaries incl. **PR diff + file content = untrusted** and `code_chunks` = attacker-influenceable on a malicious PR; 7 prompt-injection threats — coerce false "no findings" / fabricated LGTM / suppress a real finding / exfil the system prompt; 12-row mitigation table mapped to files; explicit deferrals — RBAC, secret-masking in traces, LLM-classifier layer, `code_chunks` ingestion provenance) + `backend/security/injection_guard.py` (`InjectionGuard`: per-instance random `tag = secrets.token_hex(6)`; `sanitize()` strips zero-width/bidi chars + literal `«UNTRUSTED…»` marker fragments, scans a bounded denylist — instruction-override / role-spoof / fence-breakout / verdict-steer / prompt-exfil — recording `flags` but **not deleting** the code; `wrap(label,text)` fences as `«UNTRUSTED:{tag} {label}»…«/UNTRUSTED:{tag}»`; `hardening_clause()` + `fortify()`; `GuardResult(frozen)`). Wired into `agents/base_agent.py` behind a local `SupportsPromptGuard` Protocol, **on by default** (fail-safe-on): system content = `system_prompt + hardening_clause()`, diff + each retrieved chunk routed through `guard.wrap()` instead of raw f-string interpolation. `context-graph.json` `dependency_direction` extended — `security/**` added as a cross-cutting exception alongside `observability/**`. `backend/tests/test_injection_guard.py` (5 tests: adversarial diff neutralized / benign diff byte-identical / hardening clause in system prompt / guarded prompt reaches specialist e2e / forged closing marker can't break out) + 1 assertion updated in `test_base_agent.py`. No new deps (stdlib only). L4 VERIFY (fresh checker, no build trail): APPROVE — SC1–SC5 all PASS, `pytest -q` **58 passed** (53 prior + 5 new), `mypy .` clean except the 3 known M3 `test_orchestrator.py` errors, demo 3/3. Invariants `dependency_direction` / `structured_output_only` / `outbound_call_safety` / `single_data_spine` all PASS (guard is pure, stdlib-only, no I/O, no LLM call). 3 non-blocking findings all accepted (denylist regexes can flag ordinary `system:` YAML keys / benign "approve"/"LGTM" near an output verb — acceptable since the denylist is defense-in-depth, not the primary control; the delimiting + clause is). Quiz-me: 3/3 correct (flag-not-strip so a real review still sees the code; per-instance random tag + fragment stripping defeats sentinel forgery; `agents → security` is a documented cross-cutting exception mirroring observability). **Out of M8 scope, carried forward:** RBAC / auth (no surface yet — revisit when `/api/reviews` is exposed), secret-masking in `agent_events` payloads, an LLM-classifier second layer, `code_chunks` ingestion-time provenance/signing, rate-limiting; plus the unchanged M5/M6 graph-rewiring + ARQ `run_review` worker debt and the still-unwired observability hooks. Next: M9 (eval — golden dataset + LLM-as-judge + regression gate).
- 2026-09-07 · **M7 DONE** — Minimal status frontend (deliberately thin). Boundary widened (user-approved) to add the missing read API: `backend/api/reviews.py` (`ReviewSummary` model + `create_reviews_router(get_pool)` → `GET /api/reviews`, newest-100 `pr_review_records` `LEFT JOIN LATERAL (SELECT SUM(cost_usd) FROM agent_events WHERE review_id = r.id)`, `ORDER BY created_at DESC, id` — one query, N+1 avoided, recorded deviation from the G0 "reuse `CostRepository`" note) + `backend/api/app.py` (`create_api_app` + `build_default_app` with a FastAPI `lifespan` pool — fixes the same latent `asyncio.run()`-under-`--factory` bug shape `webhook_receiver` still carries) + `backend/tests/test_reviews_api.py` (2 live-Tiger in-process ASGI tests: seeded review + 2 costed `agent_events` → asserts summed cost + confidence + status, cleans up). Frontend: hand-rolled minimal Next.js (no `create-next-app`) — `frontend/` `package.json`/`next.config.mjs`/`tsconfig.json`/`next-env.d.ts` + `src/app/{layout,page}.tsx` (async server component, `export const dynamic = "force-dynamic"`, `<h1>Reviews</h1>` + 4-col table, "No reviews yet" empty state, inline styles) + `src/lib/api.ts` (`getReviews()` → `fetch(${BACKEND_URL ?? "http://localhost:8000"}/api/reviews, {cache:"no-store"})`, try/catch → `[]`; trailing-slash-stripped). L4 VERIFY (fresh checker, no build trail): APPROVE — `mypy api` clean, `pytest -q` **53 passed** (51 prior + 2 new), `npm run build` green (`/` Dynamic), demo `curl -s localhost:3000 | grep -q "Reviews"` exit 0 with the backend down, bonus e2e SSR-rendered a real seeded review row through `/api/reviews`. Invariants: `dependency_direction` PASS (frontend only `fetch`es — no DB driver; `backend/api` imports only asyncpg/fastapi/pydantic/stdlib), `single_data_spine` PASS, `outbound_call_safety` PASS (`command_timeout=10`), `structured_output_only` n/a. 2 non-blocking findings fixed (ORDER BY tiebreaker, `BACKEND_URL` trailing slash), 1 accepted (`created_at` fetched but not rendered — keeps the type aligned with the API). Token spend ~28k (over the 20k soft cap — the missing-API boundary widen + the `build_default_app` lifespan fix added scope; flagged, not halted). Quiz-me: Q1 (lateral sum + N+1 avoidance) + Q2 (try/catch → `[]` keeps the page SSR-ing with the backend down) correct; Q3 (lifespan pool → per-request `get_pool` callable) answered after explanation. **Out of scope, carried forward:** HITL queue UI, trace viewer, economics/agent_health charts (module map lists them under `frontend/`; PLAN M7 is explicitly just the status list); styling/design-system pass; auth + `/api/reviews` exposure hardening (unauthenticated read-only on localhost); wiring the API app into one unified ASGI process alongside the webhook receiver; the M5/M6 graph-rewiring + ARQ `run_review` debt (unchanged). Next: M8.
